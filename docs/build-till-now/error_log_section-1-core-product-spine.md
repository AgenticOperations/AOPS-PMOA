---
created: 2026-07-07
project: agentOps
ecosystem: circle
tags: [section-1, error-log, identity, connections]
---

# Error Log - Section 1 Core Product Spine

Backlinks: [[10-Projects/Web3-Builds/agentOps/BUILD-PMOA/docs/build-till-now/section-1-core-product-spine]] | [[10-Projects/Web3-Builds/agentOps/BUILD-PMOA/docs/build-till-now/README]]

| Issue | Cause | Fix | Status | Prevention |
|---|---|---|---|---|
| Testcontainers could not find Docker in sandbox | Docker socket is not available inside the managed sandbox | Reran DB-backed tests and full verify with Docker escalation | Fixed | Any Testcontainers command needs Docker access |
| Section 1 tests returned `column "slug" of relation "orgs" does not exist` | Targeted API tests imported built `@agentops-pmoa/db` output, but package migrations were stale | Rebuilt packages and fixed db build script to copy migrations cleanly | Fixed | Run `npm run build:packages` before targeted API DB tests |
| Repeated package builds nested migration folders under `dist/src/migrations/migrations` | `cp -R src/migrations dist/src/migrations` copied into an existing folder | Added `packages/db/scripts/copy-migrations.mjs` to remove and recopy migrations | Fixed | Use deterministic copy scripts for generated package assets |
| Duplicate org name caused raw `orgs_slug_unique_idx` 500 | Slug was globally unique and generated directly from org name | Section 1 now keeps the clean slug when available and auto-suffixes later same-name orgs | Fixed | User-provided slugs need conflict handling before public UX |
| Exact optional TypeScript errors in API and web | Zod optional outputs include `undefined`; exact optional property types reject that unless declared | Updated store/client/action prop types to allow explicit `undefined` where Zod can produce it | Fixed | Define API input types from schema behavior, not only ideal shape |
| Next build could not resolve `.js` imports from source files | App Router bundling expected extensionless local/alias imports | Removed `.js` extensions from web source imports | Fixed | Use extensionless imports in Next source files |
| Home test failed on `server-only` | The page became an async server component that imports server-only API client code | Mocked the server API client in the page unit test and rendered the awaited component | Fixed | Server component tests should mock server-only boundaries |
| DB migration test expected only Section 11A | Test had a fixed migration list from before Section 1 | Updated expected migrations and added a `connections` table assertion | Fixed | Migration tests must evolve with every new migration |
| `npm audit` failed in sandbox | Registry network access is blocked by default | Reran `npm audit --audit-level=moderate` with network escalation | Fixed | Audit requires network access |
| API dev server failed with `listen EPERM` for the `tsx` IPC pipe | The managed sandbox blocks the local IPC pipe that `tsx watch` creates under `/var/folders` | Started the API dev server with sandbox escalation | Fixed | Dev watchers that bind local IPC or ports should run outside the sandbox |
| API boot failed with Postgres `ECONNREFUSED` | No local Postgres instance was listening on the documented `DATABASE_URL` | Created local container `agentops-pmoa-postgres` with the documented dev database/user and reran API migrations on boot | Fixed | Start the local PMOA Postgres container before running `npm run dev:api` |
| Frontend dev server failed with `listen EPERM 0.0.0.0:3005` | The managed sandbox blocks local port binding for Next dev | Started the web dev server with sandbox escalation | Fixed | Frontend dev servers should run outside the sandbox when exposing a browser port |
| `/` and `/agents` showed the same register-agent surface after org setup | The first Section 1 UI made the home route carry the operational agent workspace instead of giving `/` its own overview role | Changed `/` to an organization overview with fleet summary and kept registration inside `/agents` | Fixed | Each top-level route needs one clear job before adding new screens |
| Opening an agent detail page threw `Functions cannot be passed directly to Client Components` | `AgentDetailPage` passed an inline `rotateConnection` wrapper through a server component into the client `ConnectionPanel`; row forms also depended on generated function wrappers | Added exported form-scoped server actions and hidden `orgId` / `agentId` / resource-id fields for connection and wallet forms | Fixed | Client components should receive stable exported server actions, not ad hoc server-side function wrappers |
| Credential UI exposed `Connection type` choices too early | Section 1 showed MCP/API/observe options even though credential-level authorization and integration semantics are deferred to later sections | Replaced the selector with one neutral `agent_credential` flow and documented credential-level gating in `features-to-discuss-later.md` | Fixed | Identity screens should expose only authentication choices that are real today; authorization choices wait for policy/integration sections |
| Web auth start route required Google client ID duplication | The web BFF originally constructed the provider URL locally, which meant OAuth config had to be copied into the web env | Added `GET /v1/auth/google/authorize-url` and changed the web BFF to request the URL from the backend | Fixed | OAuth provider config belongs to the backend; the web BFF should own browser state and cookies only |
| Workspace card accessible name included the slug | The org name and slug were both visible text inside the link, so screen-reader/query name became `Acme Agent Opsacme-agent-ops` | Added an explicit `aria-label` with the org display name | Fixed | Test link accessible names when a card contains secondary metadata |
| Lint failed on async functions with no await | The new backend authorize-url route and Google fetch mock were marked `async` but returned synchronously | Made the route synchronous and changed the mock to return resolved responses | Fixed | Keep async only where a real await is needed |
| Local PMOA dev database needed a clean Section 1 state | Prior local data could make onboarding tests and manual review confusing | Dropped/recreated only the `public` schema in the `agentops` local database and reapplied migrations `0001` through `0004` | Fixed | Before handoff testing, reset only the active PMOA dev database, never old `BUILD` data |
| Final smoke briefly returned web 500 after `next build` ran while `next dev` was open | The production build rewrote `.next` artifacts while the dev server still had a stale React client manifest in memory | Stopped the stale API/web watchers, killed the orphan 8080 listener, restarted both dev servers cleanly, and reran smoke checks | Fixed | After running production builds, restart `next dev` before browser review |
| API watcher left an orphan 8080 listener during package rebuilds | `tsx watch` restarted multiple times while package `dist` files changed and one process kept the port | Killed the specific orphan PID and restarted the API watcher cleanly | Fixed | Avoid running full workspace builds while dev watchers are serving a review session; restart watchers after verify |
| Auth page looked generic and exposed fake operational copy | The first visual shell invented control-plane status cards and assurance text instead of following the actual auth/onboarding task | Replaced the shell with a video-led two-column layout, centered the left content, removed fake copy, added a real Google SVG mark, and introduced shared light/dark theme support for product pages | Fixed | Auth surfaces should show only real actions and use shared shell/theme components |
| Parallel `next build` and `tsc --noEmit` caused missing `.next/types` files | Production build regenerated `.next/types` while TypeScript was reading the include glob | Reran typecheck after build completed and kept build/typecheck sequential for verification | Fixed | Do not run `next build` and web typecheck in parallel when `.next/types` is included |
| Auth shell regressed to remote video plus setup checklist | A later edit restored the old left-card onboarding steps and external video source instead of the local PMOA asset | Changed `AuthEntryShell` to use `/AuthVideo.mp4` and render only the bottom-left strict control harness line | Fixed | Section 1 auth tests now assert local video, exact product line, and absence of the removed checklist/fake copy |
| Web root layout was syntactically broken | A later edit left duplicate `RootLayout` exports, a dangling template literal, and stale font variables | Replaced `layout.tsx` with one root layout using the CSS/system font stack and hardened theme bootstrap | Fixed | Avoid remote font dependencies and run `next build` after auth shell edits |
| Onboarding asked for unused website and primary-use-case data | The fields were stored as metadata but did not configure any Section 1 behavior or change the user's next step | Removed both fields from onboarding and made the web action submit only organization name | Fixed | Product rule added: every visible element must complete a real action, explain a real decision, or expose real state |
| Signed-in auth path skipped the account/workspace choice | The no-org `/auth` branch rendered only `Start onboarding`, and the org branch did not frame workspace opening as a direct continue choice | Changed signed-in `/auth` to always show a primary current-account/workspace continue action plus secondary `Continue with Google` | Fixed | Tests now cover signed-in no-org and signed-in with-org choice states |
| Agent registration asked for metadata that did not drive behavior | Description, labels, and environment were stored but did not affect identity creation, credentials, policy binding, filtering, or runtime behavior in Section 1 | Removed those fields from the create-agent form and made the web action submit only agent name | Fixed | Section 1 flow tests now assert agent registration exposes only required backend fields |
| Credential test/rotate/revoke actions failed with `Body cannot be empty when content-type is set to 'application/json'` | The web API client always sent `content-type: application/json`, even for empty POST actions | Changed the API client to send JSON content type only when a request body exists | Fixed | Added web API-client regression test for empty credential actions |
| Agent detail mutations refreshed the wrong route | Server actions still revalidated legacy `/agents/[agentId]` paths while the product now uses `/app/[orgSlug]/agents/[agentId]` | Added `orgSlug` to bound actions and hidden form fields, then revalidated the slug-scoped route | Fixed | Added server-action regression test for slug-scoped credential test revalidation |
| Agent detail sidebar showed only logout / missing functional navigation | The shell depended on an animated Tailwind-style sidebar component and also rendered disabled future sections | Replaced it with a CSS-only Section 1 shell that shows only workspace switch, Overview, Agents, theme toggle, and logout | Fixed | Console shell test asserts only real Section 1 navigation is rendered |
| Agent detail still showed environment and labels | The detail page retained backend metadata fields after the create-agent UI was simplified | Removed environment, default-environment, labels, and parent summary from the visible detail surface | Fixed | Agent detail and roster tests assert removed fields do not render |
| Testing a revoked credential crashed the route with `Connection was not found` | The UI rendered `Test` for revoked credentials and the server action let the backend 404 escape as a Next runtime error | Revoked connection rows are now display-only; Test/Revoke actions return controlled state errors instead of throwing | Fixed | Connection panel and action tests cover revoked rows and stale credential failures |
| Rotating a credential did not reveal the new secret | The rotate action stored the new secret only in an `sr-only` span, so the user saw only the updated last-four display | Rotation now shows the same visible one-time secret reveal pattern as credential creation | Fixed | Connection panel test clicks Rotate and asserts the full rotated secret and env line are visible |
| Credential test response looked like raw Next.js internals | Server Actions return React Server Component transport payloads in the network response, and the UI had no visible success state | `testConnectionFromFormAction` now returns `Credential test passed.` and the row renders it as a success message | Fixed | Action and panel tests assert the clean success result |
| Agent detail activity missed credential created/tested/rotated/revoked events | The agent detail query filtered only `resource_type = 'agent'`, while credential changes are stored as `resource_type = 'connection'` | Added audit event relation columns and query logic for `related_agent_id`, then classified credential events with related agent and connection IDs | Fixed | API credential test asserts agent detail history includes credential lifecycle events |
| Agent detail showed wallet references before wallet/treasury exists | Section 1 UI exposed a wallet-reference panel even though wallet creation, funding, treasury, and payment execution are not built | Removed wallet-reference panel and wallet-ref metric from agent detail; backend support remains reserved for later wallet sections | Fixed | Agent detail shell test asserts wallet reference labels are absent |
| Credential relation columns stayed null for some events | `recordIdentityEvent` options were accidentally passed in the payload slot for no-payload events like test/rotate/revoke | Passed an explicit empty payload before relation options for no-payload credential and wallet-reference events | Fixed | API credential test asserts every credential event has `related_agent_id` and `related_connection_id` |
| Evidence query type drifted after audit schema expansion | The audit writer returned classification fields but the detail/list query layer still selected the old record shape | Updated `audit-query.ts` row mapping and detail columns to include domain, category, severity, tags, and related resource IDs | Fixed | Workspace typecheck passes across API and web |
| Configuration history was noisy and showed internal connection IDs | `connection.tested` was recorded as an audit event and older credential events fell back to `related_connection_id` when display payload was missing | Stopped recording credential test checks, filtered historical `connection.tested` rows from agent history, joined connection names for old events, and removed raw resource IDs from the agent-detail activity DTO | Fixed | API and web tests assert no `connection.tested` history and no raw `conn_...` IDs in the product activity response/UI |
